# Simulator release evidence

Commit one completed JSON file named vMAJOR.MINOR.PATCH.json for each public release. Start from the adjacent template, do not include local absolute paths, and never attach the tested native libraries.

The release workflow rejects missing, placeholder, duplicate-target, target-mismatched, or non-passing evidence. Evidence is an audit record for the exact release-candidate hash, not a general compatibility promise.
